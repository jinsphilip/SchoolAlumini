MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
MONGODB_DB=school_alumni
# Long random string - keep secret. Visit the site as ?admin=<this value>
# to see and edit contact details. Generate one with:
#   node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
ADMIN_TOKEN=
# The GitHub Pages origin allowed to call this API.
ALLOWED_ORIGIN=https://jinsphilip.github.io
PORT=3000
